# AI Provider Configuration
# Set VITE_AI_PROVIDER to force a specific provider: 'mistral' | 'mock'
# If not set, defaults to mistral proxy (with runtime fallback to mock if proxy fails)
# VITE_AI_PROVIDER=mistral

# Client-visible optional model override
# VITE_MISTRAL_MODEL=mistral-small-latest

# Server-only env (set in Vercel project settings, not in client env):
# MISTRAL_API_KEY=your-mistral-api-key-here
# MISTRAL_MODEL=mistral-small-latest
